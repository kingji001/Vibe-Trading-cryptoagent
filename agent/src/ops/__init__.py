"""Ops evidence harness — 72h supervised-run artifacts + evidence report.

See :mod:`src.ops.evidence` for the cross-referenced evidence report builder
and ``scripts/ops/run72.sh`` for the supervised runner that produces the raw
``heartbeat.jsonl`` / ``supervisor.jsonl`` artifacts this package reads.
"""
