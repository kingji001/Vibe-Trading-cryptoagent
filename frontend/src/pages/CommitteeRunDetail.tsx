// Stub page — the real discussion/live-follow view lands in Task F3. This
// keeps the lazy route and TypeScript build green while the router/nav/i18n
// scaffolding (Task F1) is wired up.
export function CommitteeRunDetail() {
  return null;
}
