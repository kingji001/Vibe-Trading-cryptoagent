// Stub page — the real dashboard body lands in Task F2. This keeps the
// lazy route and TypeScript build green while the router/nav/i18n
// scaffolding (Task F1) is wired up.
export function Committee() {
  return null;
}
